import { View } from 'react-native'

import { PaperText } from '~/containers'

const Home = () => {
  return (
    <View>
      <PaperText>Home</PaperText>
    </View>
  )
}

export default Home
